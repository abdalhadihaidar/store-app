<!DOCTYPE html>
<html lang="de">
<head>
  <meta charset="UTF-8" />
  <title>Angebot</title>
  <style>
    * { margin: 0; padding: 0; box-sizing: border-box; }
    
    html, body { 
      font-family: Arial, sans-serif; 
      font-size: 14px; 
      line-height: 1.4; 
      color: #333;
      margin: 0 !important;
      padding: 0 !important;
      min-height: 100vh;
      position: relative;
      width: 100%;
      max-width: 210mm; /* A4 width */
      margin: 0 auto !important;
      overflow-x: hidden;
    }
    
    /* A4 page dimensions - PDF optimized */
    .a4-page {
      width: 210mm;
      min-height: 297mm; /* A4 height */
      max-height: 297mm;
      margin: 0 auto;
      padding: 20mm 15mm 15mm 15mm; /* Top, Right, Bottom, Left - proper A4 margins */
      box-sizing: border-box;
      background: white;
      position: relative;
      page-break-after: always;
      page-break-inside: avoid;
      display: flex;
      flex-direction: column;
    }
    
    .a4-page:last-child {
      page-break-after: avoid;
    }
    
    .angebot { 
      padding: 0; 
      margin: 0; 
      min-height: 100%;
      position: relative;
      display: flex;
      flex-direction: column;
      flex: 1;
    }
    
    .container { 
      max-width: 100%; 
      margin: 0 auto; 
      padding: 0; 
      flex: 1;
    }
    
    .row { 
      display: flex; 
      flex-wrap: wrap; 
      margin: 0 -15px; 
    }
    
    .col-9 { 
      flex: 0 0 75%; 
      max-width: 75%; 
      padding: 0 15px; 
    }
    
    .col-3 { 
      flex: 0 0 25%; 
      max-width: 25%; 
      padding: 0 15px; 
    }
    
    .col-4 { 
      flex: 0 0 33.333333%; 
      max-width: 33.333333%; 
      padding: 0 15px; 
    }
    
    .col-6 { 
      flex: 0 0 50%; 
      max-width: 50%; 
      padding: 0 15px; 
    }
    
    .col-1 { 
      flex: 0 0 8.333333%; 
      max-width: 8.333333%; 
      padding: 0 15px; 
    }
    
    .col-2 { 
      flex: 0 0 16.666667%; 
      max-width: 16.666667%; 
      padding: 0 15px; 
    }
    
    .col-5 { 
      flex: 0 0 41.666667%; 
      max-width: 41.666667%; 
      padding: 0 15px; 
    }
    
    .mt-4 { margin-top: 0.8rem; }
    .mt-5 { margin-top: 1.5rem; }
    .mb-3 { margin-bottom: 0.5rem; }
    .my-5 { margin-top: 1.5rem; margin-bottom: 1.5rem; }
    .pt-2 { padding-top: 0.5rem; }
    .p-2 { padding: 0.5rem; }
    .pt-2 { padding-top: 0.5rem; }
    .pb-2 { padding-bottom: 0.5rem; }
    
    .text-left { text-align: left; color: black; font-weight: 600; }
    .text-right { text-align: right; color: #d2691e; font-weight: 600; font-size: 15px; }
    .text-right-left { text-align: left; color: #d2691e; font-weight: 600; font-size: 15px; line-height: 1.6; }
    .text-center { text-align: center; }
    
    .address { 
      color: #d2691e; 
      font-weight: bold; 
      font-size: 15px; 
      white-space: pre; 
      text-decoration: underline; 
      margin: 0 !important; 
      padding: 0 !important; 
      line-height: 1.6;
    }
    
    .kk .text-left span { 
      margin: 0 !important; 
      padding: 0 !important; 
      line-height: 1.0;
    }
    
    .text-right img { 
      width: 110px; 
      height: 100px; 
      object-fit: contain; 
      image-rendering: crisp-edges; 
      padding-bottom: 10px; 
    }
    
    .angebot-header { 
      position: relative; 
      padding-top: 5px; 
      margin-top: 5px; 
    }
    
    .Angebot { 
      color: white; 
      border: 5px solid #d2691e; 
      background-color: #d2691e; 
      letter-spacing: 4px; 
      font-weight: bold; 
      border-radius: 15px; 
      width: 50%; 
      padding: 0px 12px; 
      text-align: center; 
      z-index: 1000; 
      position: absolute; 
      top: -20px; 
      left: 0;
    }
    
    .angebot-row { 
      border: 3px solid #d2691e; 
      background-color: white; 
      border-radius: 15px;  
      
    }
    
    .angebot-row p { 
      margin-bottom: 0px; 
      line-height: 1.0;
    }
    
    .angebot-row strong { 
      color: #d2691e; 
    }
    
    table { 
      width: 100%; 
      border-collapse: collapse; 
      margin-bottom: 5px; 
      margin-top: 1rem;
    }
    
    table thead .firsttr th { 
      background-color: #f2f2f2; 
      border-top: 2px solid black; 
      font-size: small; 
      padding: 8px; 
      text-align: center; 
    }
    
    table thead .sectr th { 
      border-bottom: 2px solid black; 
      background-color: #f2f2f2; 
      padding: 8px; 
      text-align: center; 
      line-height: 0.5; 
      font-size: 10px; 
    }
    
    table th, table td { 
      padding: 1px 2px; 
      text-align: center; 
      border-bottom: 0.5px solid #ddd; 
      vertical-align: middle;
    }
    
    table td { 
      font-size: smaller; 
    }
    
    table tbody tr:last-child td { 
      border-bottom: none; 
    }
    
    footer { 
      position: relative;
      width: 100%;
      background: white;
      border-top: 2px solid #ae1308; 
      font-weight: bold; 
      font-size: 11px; 
      padding: 1rem 0;
      margin: 0;
      min-height: 50mm; /* Minimum height for calculation summary */
      page-break-inside: avoid; /* Prevent footer from breaking across pages */
      margin-top: auto; /* Push footer to bottom of page */
      flex-shrink: 0; /* Prevent footer from shrinking */
    }
    
    footer .flex { 
      display: flex; 
      justify-content: space-around; 
    }
    
    /* Remove fixed positioning for better A4 alignment */
    
    footer .justify-content-around { 
      justify-content: space-around; 
    }
    
    .signature-line { 
      border-top: 2px solid black; 
      min-height: 40px; 
      margin-top: 8px; 
      padding-top: 8px; 
    }
    
    .bank-info { 
      border-top: 3px solid #ae1308; 
      margin-top: 1rem; 
      color: #ae1308; /* Red color for bank details */
      font-weight: bold;
    }
    
    .bank-info span { 
      color: #ae1308; 
      font-weight: bold;
    }
    
    .bank-info p { 
      color: #ae1308; 
      font-weight: bold;
    }
    
    .total-section { 
      border-top: 3px solid #ae1308; 
      padding-top: 10px; 
      margin-top: 1rem; 
    }
    
    .total-amount { 
      font-weight: bold; 
      font-size: 13px; 
      color: black; 
    }
    
    @media print {
      @page {
        size: A4;
        margin: 0;
      }
      * {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }
      html, body { 
        font-size: 12px; 
        width: 210mm;
        height: 297mm;
        margin: 0;
        padding: 0;
        overflow: hidden;
      }
      .a4-page {
        width: 210mm;
        height: 297mm;
        margin: 0;
        padding: 20mm 15mm 15mm 15mm; /* Proper A4 print margins */
        box-sizing: border-box;
        page-break-after: always;
        page-break-inside: avoid;
        display: flex;
        flex-direction: column;
      }
      .a4-page:last-child {
        page-break-after: avoid;
      }
      .angebot { 
        padding: 0;
        margin: 0;
        max-width: none;
        width: 100%;
        height: 100%;
        display: flex;
        flex-direction: column;
      }
      .container {
        padding: 0;
        width: 100%;
        flex: 1;
      }
      .mt-4 { margin-top: 0.5rem; }
      .mt-5 { margin-top: 1rem; }
      .my-5 { margin-top: 1rem; margin-bottom: 1rem; }
      .signature-line { 
        min-height: 20px; 
        margin-top: 3px; 
        padding-top: 3px; 
        font-size: 10px;
      }
      footer { 
        position: relative;
        width: 100%;
        page-break-inside: avoid; 
        margin: 0;
        padding: 0.5rem 0;
        font-size: 10px;
        border-top: 2px solid #ae1308;
        margin-top: auto;
        min-height: 40mm;
        flex-shrink: 0;
      }
      table { 
        page-break-inside: auto; 
        margin-bottom: 5px; 
        margin-top: 0.5rem;
        font-size: 10px;
        width: 100%;
      }
      tr { 
        page-break-inside: avoid; 
        page-break-after: auto; 
      }
      thead { 
        display: table-header-group; 
        page-break-inside: avoid;
        page-break-after: avoid;
      }
      tfoot { 
        display: table-footer-group; 
      }
      .angebot-header {
        page-break-inside: avoid;
        page-break-after: avoid;
      }
      .angebot-row {
        page-break-inside: avoid;
        page-break-after: avoid;
      }
      .total-amount { font-size: 12px; }
      .bank-info { 
        font-size: 9px;
        margin-top: 10px;
        padding-top: 8px;
        page-break-inside: avoid;
      }
      .bank-info p { 
        margin-bottom: 2px;
        line-height: 1.2;
      }
    }
  </style>
</head>
<body>
  <div class="a4-page">
    <div class="angebot">
    <div class="container mt-4 kk">
      <div class="row">
        <div class="col-9 text-left">
          <h6 class="address">Brother Investment Group Horburger str.97 04205 Leipzig-Deutschland</h6>
          <span><%= order.store ? order.store.name : 'N/A' %><br/></span>
            <% if (order.store && order.store.address) { %>
            <span><%= order.store.address %><br/></span>
            <span><%= order.store.city %><br/></span>
            <span><%= order.store.postalCode %><br/></span>
          <% } %>
        </div>
        <div class="col-3 text-right">
          <img src="Capture.png" alt="Logo" />
          <div class="text-right-left">
            <span>Horburger str.97</span><br/>
            <span>04205 Leipzig</span><br/>
            <span>Steuer-ID: 232/200/25047</span>
          </div>
        </div>
      </div>

      <div class="angebot-header">
        <h3 class="Angebot">ANGEBOT :</h3>
        <div class="angebot-border">
          <div class="row angebot-row">
            <div class="col-3 mt-4 mb-3" style="font-weight: bold;">
              <p><strong>Bestellung vom:</strong><br/><%= new Date(order.createdAt).toLocaleDateString('de-DE') %></p>
            </div>
            <div class="col-3 mt-4 mb-3" style="font-weight: bold;">
              <p><strong>Angebotsnr:</strong><br/><%= angebot.angebotNumber || 'ANG-' + angebot.id %></p>
            </div>
            <div class="col-3 mt-4 mb-3" style="font-weight: bold;">
              <p><strong>Angebotsdatum:</strong><br/><%= new Date(angebot.date).toLocaleDateString('de-DE') %></p>
            </div>
            <div class="col-3 mt-4 mb-3" style="font-weight: bold;">
              <p><strong>Kunden-Nr.</strong><br/><%= order.user ? order.user.id : 'N/A' %></p>
            </div>
          </div>
        </div>
      </div>

      <table class="mt-4" style="margin-bottom: 50px;">
        <thead>
          <tr class="firsttr">
            <th style="width: 8%;">Artikel-Nr</th>
            <th style="width: 25%;">Artikel</th>
            <th style="width: 8%;">Anzahl pro Paket</th>
            <th style="width: 8%;">Menge</th>
            <th style="width: 10%;">Netto-Einzelpreis</th>
            <th style="width: 10%;">Brutto-Gesamtpreis</th>
            <th style="width: 8%;">7%</th>
            <th style="width: 8%;">19%</th>
            <th style="width: 15%;">enthaltene MwSt</th>
          </tr>
        </thead>
        <tbody>
          <% items.forEach(function(item){ %>
            <tr>
              <td><%= item.orderProduct ? item.orderProduct.id : item.id %></td>
              <td><%= item.productName || item.orderProduct?.name || 'N/A' %></td>
              <td><%= item.unitPerPackageSnapshot || 1 %></td>
              <td><%= item.quantity %></td>
              <td><%= (item.adjustedPrice || item.originalPrice).toFixed(2) %></td>
              <td><%= ((item.adjustedPrice || item.originalPrice) * item.quantity).toFixed(2) %></td>
              <% 
                const taxRate = item.taxRate || 19;
                const itemTotal = (item.adjustedPrice || item.originalPrice) * item.quantity;
                const tax7 = taxRate === 7 ? (itemTotal * 0.07) : 0;
                const tax19 = taxRate === 19 ? (itemTotal * 0.19) : 0;
              %>
              <td><%= tax7.toFixed(2) %>€</td>
              <td><%= tax19.toFixed(2) %>€</td>
              <td><%= (tax7 + tax19).toFixed(2) %>€</td>
            </tr>
          <% }); %>
        </tbody>
      </table>

    </div>
  </div>

  <footer>
    <!-- Financial Summary - Every Page -->
    <div class="row align-items-center mt-2">
      <div class="col-6 mt-2 mb-3">
        <p>Es gelten stets die Allgemeinen Geschäftsbedingungen :</p>
      </div>
      <div class="col-4 mt-2 mb-3">
        <p>Netto-Gesamtbestellsumme:</p>
        <p>zuzüglich 7% Mehrwertsteuer:</p>
        <p>zuzüglich 19% Mehrwertsteuer:</p>
      </div>
      <div class="col-2">
        <p><%= angebot.totalNet.toFixed(2) %>€</p>
        <p><%= angebot.tax7Amount.toFixed(2) %>€</p>
        <p><%= angebot.tax19Amount.toFixed(2) %>€</p>
      </div>
    </div>
    
    <div class="row align-items-center">
      <div class="col-6"></div>
      <div class="col-4 total-section mt-2">
        <h3 class="mt-2 pt-2 total-amount">Angebotsbetrag:</h3>
      </div>
      <div class="col-2 total-section mt-2">
        <h3 class="mt-2 pt-2 total-amount"><%= angebot.totalGross.toFixed(2) %>€</h3>
      </div>
    </div>
    
    <!-- Signatures - Every Page -->
    <div class="row flex justify-content-around mt-2">
      <div class="col-4 signature-line">
        <span>Unterschrift Kunde / Angebot angenommen:</span>
      </div>
      <div class="col-4 signature-line">
        <span>Unterschrift Verkäufer / Angebot erstellt:</span>
      </div>
      <div class="col-4"></div>
    </div>
    
    <!-- Validity Information - Only on last page (or single page) -->
    <% 
      const totalPages = Math.ceil(items.length / 21);
      const pageNumber = typeof currentPage !== 'undefined' ? currentPage : 1;
      const isLastPage = pageNumber === totalPages;
    %>
    <% if (isLastPage) { %>
    <div class="row flex my-5 justify-content bank-info">
      <div class="col-1"></div>
      <div class="col-5 mb-3 mt-3">
        <span>Gültigkeitsdauer:</span><br/>
        <span>Annahme:</span><br/>
        <span>Preise:</span><br/>
        <span>Zahlungsbedingungen:</span><br/>
        <span>Lieferbedingungen:</span>
      </div>
      <div class="col-5 mb-3 mt-3">
        <span>Dieses Angebot ist gültig bis zum <%= new Date(angebot.validUntil).toLocaleDateString('de-DE') %></span><br/>
        <span>Das Angebot wird durch Unterschrift oder schriftliche Bestätigung angenommen</span><br/>
        <span>Alle Preise verstehen sich inklusive der gesetzlichen Mehrwertsteuer</span><br/>
        <span>Zahlung innerhalb von 14 Tagen nach Lieferung</span><br/>
        <span>Lieferung erfolgt nach Annahme des Angebots</span>
      </div>
      <div class="col-1"></div>
    </div>
    <% } %>
  </footer>
    </div>
  </div>
</body>
</html>
